# Dust Protocol - Environment Variables
# Network: Thanos Sepolia (chain ID: 111551119090)

# Stealth Name Registry Address
NEXT_PUBLIC_STEALTH_NAME_REGISTRY_ADDRESS=0x857e17A85891Ef1C595e51Eb7Cd56c607dB21313

# Sponsor / Relayer Key
# Used by Next.js API routes for:
#   - ERC-4337 paymaster signatures + handleOps() bundling
#   - Legacy sponsor-claim relay
#   - DustPool deposit/withdraw relay
#   - On-chain announcements + name registrations
RELAYER_PRIVATE_KEY=your_relayer_private_key_here

# Gelato 1Balance API key for gasless relay (server-side only)
GELATO_API_KEY=

# Relayer URL (defaults to http://localhost:3001 if not set)
NEXT_PUBLIC_RELAYER_URL=http://localhost:3001

# Privy Social Login (optional)
# Create an app at https://console.privy.io to get your App ID
# Enables Google, Email, and Farcaster login via embedded wallets
NEXT_PUBLIC_PRIVY_APP_ID=your_privy_app_id_here

# The Graph — Subgraph Query URLs (optional)
# Deploy subgraphs to Subgraph Studio, then paste query URLs here.
# See docs/GRAPH_DEPLOYMENT.md for setup instructions.
# Format: https://api.studio.thegraph.com/query/<STUDIO_ID>/<SUBGRAPH_NAME>/version/latest
NEXT_PUBLIC_SUBGRAPH_URL_THANOS=
NEXT_PUBLIC_SUBGRAPH_URL_SEPOLIA=https://api.studio.thegraph.com/query/1741961/dust-protocol-sepolia/v0.0.2

# Feature flag: use The Graph for name queries instead of RPC calls
# Set to 'true' after deploying subgraphs and confirming sync is complete
NEXT_PUBLIC_USE_GRAPH=false

# Alchemy RPC endpoint for Sepolia (recommended for high rate limits)
# Get free endpoint at: https://www.alchemy.com/
NEXT_PUBLIC_ALCHEMY_SEPOLIA_RPC=https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# V2 FFLONK proving key URL (optional)
# The zkey is 107MB — too large for git. Host on Cloudflare R2 or similar CDN.
# If not set, falls back to /circuits/v2/DustV2Transaction.zkey (local public dir)
NEXT_PUBLIC_V2_ZKEY_URL=
